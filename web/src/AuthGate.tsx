import {useState,type FormEvent} from 'react';import App from './App';import {requestCode,verifyCode} from './api';import './auth.css';
export default function AuthGate(){const [tenantId,setTenantId]=useState('');const [email,setEmail]=useState('');const [code,setCode]=useState('');const [sent,setSent]=useState(false);const [token,setToken]=useState<string|null>(null);const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{if(!sent){await requestCode(tenantId,email);setSent(true)}else{setToken((await verifyCode(tenantId,email,code)).accessToken)}}catch(err){setError(err instanceof Error?err.message:'Unable to sign in')}finally{setBusy(false)}}if(token)return <App token={token}/>;return <div className="loginPage"><form className="loginCard" onSubmit={submit}><div className="brand loginBrand"><span className="brandMark">L</span><div><strong>Llamar</strong><small>Secure Control Center</small></div></div><h1>Sign in to your workspace</h1><p>{sent?'Enter the six-digit code sent to your approved email.':'Use your tenant ID and approved work email.'}</p><label>Tenant ID<input required value={tenantId} onChange={e=>setTenantId(e.target.value)} disabled={sent}/></label><label>Work email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} disabled={sent}/></label>{sent&&<label>Verification code<input required inputMode="numeric" pattern="[0-9]{6}" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))}/></label>}{error&&<div className="formError">{error}</div>}<button className="primary loginButton" disabled={busy}>{busy?'Please wait…':sent?'Verify and sign in':'Send verification code'}</button>{sent&&<button type="button" className="linkButton" onClick={()=>setSent(false)}>Use another account</button>}</form></div>}
