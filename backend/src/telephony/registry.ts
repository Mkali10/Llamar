import {TwilioAdapter} from './twilio.js';import {TelnyxAdapter} from './telnyx.js';import {PlivoAdapter} from './plivo.js';import type {TelephonyAdapter} from './types.js';
export function telephonyAdapter(provider:string):TelephonyAdapter{if(provider==='twilio')return new TwilioAdapter({accountSid:process.env.TWILIO_ACCOUNT_SID??'',authToken:process.env.TWILIO_AUTH_TOKEN??'',defaultFrom:process.env.TWILIO_DEFAULT_FROM??''});if(provider==='telnyx')return new TelnyxAdapter({apiKey:process.env.TELNYX_API_KEY??'',connectionId:process.env.TELNYX_CONNECTION_ID??'',defaultFrom:process.env.TELNYX_DEFAULT_FROM??''});if(provider==='plivo')return new PlivoAdapter({authId:process.env.PLIVO_AUTH_ID??'',authToken:process.env.PLIVO_AUTH_TOKEN??'',defaultFrom:process.env.PLIVO_DEFAULT_FROM??''});throw new Error(`telephony provider execution not implemented: ${provider}`)}
