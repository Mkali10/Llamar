import assert from 'node:assert/strict';import test from 'node:test';import {PlivoAdapter} from './plivo.js';
test('Plivo adapter launches outbound call with answer and lifecycle URLs',async()=>{let sent:Record<string,unknown>={};const fakeFetch:typeof fetch=async(_input,init)=>{sent=JSON.parse(String(init?.body));return new Response(JSON.stringify({request_uuid:'plivo-request-id'}),{status:201,headers:{'content-type':'application/json'}})};const adapter=new PlivoAdapter({authId:'MAXTEST',authToken:'secret',defaultFrom:'+14155550100'},fakeFetch);const result=await adapter.createCall({to:'+919876543210',instructionUrl:'https://voice.example.com/v1/twiml/plivo/outbound',statusCallbackUrl:'https://voice.example.com/v1/webhooks/plivo/status',timeoutSeconds:30});assert.equal(result.provider,'plivo');assert.equal(sent.answer_method,'POST');assert.equal(sent.hangup_url,'https://voice.example.com/v1/webhooks/plivo/status')});
