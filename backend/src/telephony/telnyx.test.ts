import assert from 'node:assert/strict';import test from 'node:test';import {TelnyxAdapter} from './telnyx.js';
test('Telnyx adapter starts bidirectional PCMU media streaming',async()=>{let sent:Record<string,unknown>={};const fakeFetch:typeof fetch=async(_input,init)=>{sent=JSON.parse(String(init?.body));return new Response(JSON.stringify({data:{call_control_id:'v2:test-control-id',is_alive:true}}),{status:200,headers:{'content-type':'application/json'}})};const adapter=new TelnyxAdapter({apiKey:'KEY',connectionId:'CONNECTION',defaultFrom:'+14155550100'},fakeFetch);const result=await adapter.createCall({to:'+442071838750',streamUrl:'wss://voice.example.com/media?token=test',statusCallbackUrl:'https://voice.example.com/status',timeoutSeconds:30});assert.equal(result.provider,'telnyx');assert.equal(sent.stream_bidirectional_mode,'rtp');assert.equal(sent.stream_bidirectional_codec,'PCMU');assert.equal(sent.stream_track,'inbound_track')});
