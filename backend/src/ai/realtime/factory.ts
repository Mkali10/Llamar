import {OpenAIRealtimeProvider} from './openai.js';import {GeminiRealtimeProvider} from './gemini.js';import {XaiRealtimeProvider} from './xai.js';import {NvidiaRealtimeProvider} from './nvidia.js';import type {RealtimeCallbacks,RealtimeConfig,RealtimeProvider,RealtimeProviderKey} from './types.js';
export function createRealtimeProvider(key:RealtimeProviderKey,config:RealtimeConfig,callbacks:RealtimeCallbacks):RealtimeProvider{if(key==='gemini')return new GeminiRealtimeProvider(config,callbacks);if(key==='xai')return new XaiRealtimeProvider(config,callbacks);if(key==='nvidia')return new NvidiaRealtimeProvider(config,callbacks);return new OpenAIRealtimeProvider(config,callbacks)}
export function normalizeRealtimeProvider(value:unknown):RealtimeProviderKey{if(value==='gemini'||value==='xai'||value==='nvidia')return value;return'openai'}
