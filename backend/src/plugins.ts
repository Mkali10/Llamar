import {z} from 'zod';import type pg from 'pg';
export const PluginCreate=z.object({category:z.enum(['telephony','stt','tts','llm','realtime_ai','avatar','calendar','meeting']),providerKey:z.string().min(2).max(80),displayName:z.string().min(2).max(100),secretRef:z.string().min(3).max(240),capabilities:z.array(z.string().regex(/^[a-z0-9_.:-]{2,100}$/)).max(100),configuration:z.record(z.string(),z.unknown()).default({})});
export async function createPlugin(client:pg.PoolClient,tenantId:string,input:z.infer<typeof PluginCreate>,actorId:string){const r=await client.query(`INSERT INTO provider_connections(tenant_id,category,provider_key,display_name,secret_ref,capabilities,configuration) VALUES($1,$2,$3,$4,$5,$6,$7::jsonb) RETURNING id,category,provider_key AS "providerKey",display_name AS "displayName",capabilities,status,created_at AS "createdAt"`,[tenantId,input.category,input.providerKey,input.displayName,input.secretRef,input.capabilities,JSON.stringify(input.configuration)]);await client.query(`INSERT INTO audit_events(tenant_id,actor_id,action,resource_type,resource_id,payload) VALUES($1,$2,'plugin.created','provider_connection',$3,$4::jsonb)`,[tenantId,actorId,r.rows[0].id,JSON.stringify({providerKey:input.providerKey,category:input.category})]);return r.rows[0]}
export async function listPlugins(client:pg.PoolClient){return {items:(await client.query(`SELECT id,category,provider_key AS "providerKey",display_name AS "displayName",capabilities,status,created_at AS "createdAt",updated_at AS "updatedAt" FROM provider_connections ORDER BY created_at DESC`)).rows}}
