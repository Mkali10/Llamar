import type pg from 'pg';import {liveCalls} from './liveCalls.js';
export async function operationalOverview(c:pg.PoolClient,tenantId:string){const [tenant,capacity,calls]=await Promise.all([c.query(`SELECT display_name AS "displayName",status FROM tenants WHERE id=$1`,[tenantId]),c.query(`SELECT(SELECT count(*)::int FROM campaign_call_jobs WHERE state IN('leased','dialing','ringing','answered','wrap_up')) AS "activeChannels",(SELECT COALESCE(sum(channel_limit),0)::int FROM campaigns WHERE status IN('ready','running','paused')) AS "configuredChannels",(SELECT COALESCE(sum(used_slots),0)::int FROM campaign_cps_slots WHERE slot_second=date_trunc('second',clock_timestamp())) AS "currentCps",(SELECT COALESCE(sum(cps_limit),0)::numeric FROM campaigns WHERE status IN('ready','running','paused')) AS "configuredCps"`),liveCalls(c,5)]);const workspace=tenant.rows[0];if(!workspace)throw new Error('tenant not found');return {workspace,...capacity.rows[0],recentCalls:calls.items}}
